extends: 
  - spectral:oas

rules:
  # Spectral cannot validate OpenAPI 3.1 role names for non-OAuth schemes
  # or resolve OpenID Connect scopes from the discovery document.
  oas3-operation-security-defined: off